# JSON object format (recommended). Add as many ID/password pairs as needed.
LOGIN_CREDENTIALS='{"operator":"change-me","viewer":"change-me-too"}'

# Use a long, random value (for example: openssl rand -base64 48).
AUTH_SESSION_SECRET='replace-with-a-long-random-secret'

# Existing server configuration.
NEXT_PUBLIC_BACKEND_SERVER='https://172.30.1.77:8099'
LIVEKIT_SIGNAL_SERVER='ws://172.30.1.77:7880'
